// FIXTURE de prueba, no es código de producción.
// Un `loading.tsx` cualquiera: para Next el NOMBRE ya es el límite de
// Suspense, da igual lo que haya dentro. El guard debe marcarlo por nombre.
export default function Cargando() {
  return null
}
