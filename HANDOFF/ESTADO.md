# Estado de los bloques

Cada sesión actualiza **solo su propia fila** al terminar. Añadir una fila nueva
está bien; reescribir la fila de otro, no.

| Bloque | Título | Estado | Sesión / fecha | Notas |
|---|---|---|---|---|
| F1 | Migración núcleo (`0001_core.sql`) | ✅ hecho | 2026-08-03 | Identidad, karma, reciprocidad por trigger, RLS |
| F2 | Migración comunidad (`0002_comunidad.sql`) | 🟡 en curso | 2026-08-03 | |
| F3 | Núcleo `lib/**` | 🟡 en curso | 2026-08-03 | |
| F4 | Configuración, CSP y `app/` base | 🟡 en curso | 2026-08-03 | |
| B00 | Integración y contratos | ✅ hecho | 2026-08-03 | `HANDOFF/**` |
| B01 | Auth anónima y onboarding | ✅ hecho | 2026-08-03 | `tsc`/`eslint` limpios en mis archivos, 49 tests verdes. Migración `0101_b01_auth.sql` **escrita pero NO aplicada** (sin Docker ni proyecto Supabase). Pedidos abiertos: `/api/auth/*` público en `proxy.ts` (F4) y `IDENTITY_PEPPER`/`TOTP_ENC_KEY` en `.env.example` (F4) |
| B02 | Feed «Para ti» | ✅ hecho | 2026-08-03 | `app/(app)/feed/**`, `app/api/feed/**`, `components/feed/**` + migración **`0102_1_feed_keyset.sql` APLICADA** en `darma-dev` (4 RPC keyset `security invoker`; cero índices nuevos — los de 0001/0002 bastan). `tsc` y `eslint` limpios, 36 pruebas verdes (14 de fallo). **Medido de verdad** con 90 000 posts sembrados y borrados después: `Index Scan using idx_posts_hot`, `rows=20`, sin `Sort`; página 1 **2,33 ms**, página 200 **2,45 ms** (con `Index Cond: ROW(hot_score, id) < ROW(...)`), página 3 500 **3,12 ms** → coste constante con la profundidad. Nota de nombre: la ficha decía `0004_b02_feed.sql`, pero ese número ya estaba ocupado; se usa el rango `0102x` que manda `PARALELO.md` §3 |
| B03 | Publicar y gate de reciprocidad | ✅ hecho | 2026-08-03 | `tsc` y `eslint` limpios en lo propio, 31 pruebas verdes. Migración `0103_1_b03_publicar.sql` **aplicada** en `darma-dev` y **verificada contra Postgres**: primer post gratis, 2º sin escuchas → `23514 reciprocidad:`, `23514` de longitud distinguido por mensaje, post `critical` queda `active` + fila en `crisis_events` en la misma transacción, `risk` solo escala en el PATCH, RPC inalcanzable para `authenticated` (42501). Datos de prueba borrados. ⚠️ **Decisión que merece revisión:** el INSERT va por una RPC `security definer` concedida solo a `service_role`, no por el cliente RLS — razonado en la cabecera de la migración. ⚠️ **Bloqueante ajeno:** la política `posts_read` de 0001 hace que `authenticated` NO pueda leer `posts` (42501); rompe el feed entero, ver PEDIDOS. `SUPABASE_SERVICE_ROLE_KEY` vacía en `.env.local` |
| B04 | Hilo: escuchar y validar | ✅ hecho | 2026-08-03 | `app/(app)/post/**`, `app/api/comments/**`, `components/thread/**`. `tsc` y `eslint` limpios en lo propio, 15 pruebas verdes. Migraciones `0104_1_hilo.sql` (realtime + `idx_comments_post_keyset` + la política `comments_update_own` que faltaba) y `0104_2_marcar_util.sql` (traslado de «me ayudó» en una transacción) **aplicadas** en `darma-dev`. Ciclo verificado CONTRA POSTGRES: comentario nace `is_validated=false` (el cliente RLS ni siquiera puede insertar la columna), validar con admin paga **+10 reputación, +3 gastable, +1 crédito, +1 `listens_given`, `reply_count` 1**; segundo comentario en el mismo post → `23505`; «me ayudó» paga **+15 al comentarista y 0 al autor del post**; un like no genera ninguna fila en `karma_events`; con `daily_karma_earned=118` se pagan **2, no 10**. `EXPLAIN` del keyset: `Index Scan using idx_comments_post_keyset`, sin filtro sobre heap. Datos de prueba borrados |
| B05 | Perfil, karma y niveles | ✅ hecho | 2026-08-03 | `tsc --noEmit` limpio en todo el repo, `eslint` limpio en lo propio, 36 tests verdes (8 casos de fallo). Migración `0105_1_b05_perfil.sql` **APLICADA** contra `darma-dev`: índice keyset del ledger, racha en columna + trigger, `mi_resumen_karma()` (definer) y `mi_historial_karma()` (**invoker**, para conservar `karma_events_read_own` como segunda barrera). Verificado con **dos sesiones reales**: 16/16 en verde — un usuario NO puede leer `karma_spendable`, `crystals`, `streak_days`, `listens_given`, `posts_published`, `shadow_banned` ni `banned_until` de otro (403/42501), ni los suyos con un `select`; su ledger forzado por PostgREST devuelve `[]`. `EXPLAIN` confirma `Index Scan using idx_karma_events_user_keyset` con la tupla en el `Index Cond`. Datos de prueba borrados. ⚠️ **Dos desviaciones de la ficha, ambas por el endurecimiento del esquema**: `PerfilAjeno` NO lleva `escuchasDadas` ni `publicaciones` (`authenticated` ya no tiene SELECT sobre esas columnas y no se les devolvió el grant), y el perfil propio son **4 consultas**, no 3. Rate limiting solo en capa de memoria (la capa 2 exige el cliente admin, prohibido en este bloque). Las tres cosas en `PEDIDOS.md` |
| B06 | Ranking y reconocimiento | ✅ hecho | 2026-08-03 | `app/(app)/ranking/**`, `app/api/ranking/**`, `components/ranking/**`, `lib/ranking/**`. `tsc --noEmit --incremental false` limpio en lo propio (los 2 errores restantes del repo son de `app/api/push/prefs/route.ts`, B13), `eslint` limpio, **92 pruebas verdes** (37 de camino de fallo). Migraciones `0106_1/2/3` **APLICADAS** en `darma-dev` (rango `0106x` de `PARALELO.md`; la ficha decía `0006`, ya ocupado). Dos tablas: `listen_daily` (rollup diario por trigger, **cero políticas RLS** y `revoke all` — el ritmo diario de alguien es un dato de conducta) y `ranking_snapshots` (la foto, `select` enumerado por columnas, escritura revocada entera). **Ningún privilegio de `profiles` reconcedido**: la métrica sale de `ranking_snapshots.listens`, no de `listens_given`. ⚠️ **DOS BUGS EN EL SQL DE LA FICHA, encontrados midiendo**: (a) `dense_rank() over (order by listens desc, user_id)` no empata nunca — medido `max(rank)=count(*)` con 97 696 personas; corregido en `0106_3`, ahora el puesto 1 lo comparten 13 786 personas con 36 escuchas; (b) el keyset `where rank > :cursor_rank` se come a los empatados — medido: **2 142 personas desaparecen justo tras la página 1** en un corte de 100 003; se pagina por la tupla `(rank, user_id)` y el índice es `(period, period_start, rank, user_id)`. **Medido de verdad** con 100 006 perfiles, 951 257 filas en `listen_daily` y **1 034 707 filas en `ranking_snapshots`** (17 cortes): tablero página 1 `Index Scan using idx_ranking_board`, 92 buffers, **1,78 ms**; página 4 500 (fila 90 000, en mitad de un grupo de empatados) 94 buffers, **0,50 ms** → coste constante con la profundidad, sin `Seq Scan` y sin `Sort`. `ranking_fila` es `Index Scan using ranking_snapshots_pkey`. Construcción de un corte de 100 003 personas: 16,4 s (cabe de sobra en el presupuesto de 50 s). Suite de intrusión con rol `authenticated` REAL: **12/12** — `listen_daily` SELECT e INSERT 42501, `ranking_snapshots` INSERT/UPDATE/DELETE 42501, `construir_ranking_snapshot` 42501, y siguen cerradas `listens_given`, `shadow_banned` y `karma_spendable` (incluso la propia). Verificado contra Postgres: techo antifarmeo (300 escuchas en un día → 12), shadow-baneado y baneado excluidos **sin dejar hueco en la numeración**, silenciar a alguien que ya estaba en la foto lo saca al reconstruir, `prev_rank`/movimiento (+2, +1, −1, nuevo), idempotencia (0 filas cambian en dos reconstrucciones), trigger `false→true` suma / `true→false` RESTA / `DELETE` resta, y el reloj de negocio (comentario a las 22:30Z del 3 de agosto cuenta como día 4 en Madrid). **Datos de prueba borrados** (las dos tablas a 0 filas, 100 006 perfiles y usuarios eliminados). ⚠️ **Bloqueante ajeno**: `/api/ranking/snapshot` NO está en las rutas públicas de `proxy.ts`, así que hoy el cron recibe un 401 del proxy antes de llegar al Bearer — ver PEDIDOS. ⚠️ `darma-dev` se quedó **sin disco** durante la medición (B09 + B06 a la vez); ver PEDIDOS. Pendiente de medir: KB de JS y LCP reales (exigen `next build`, prohibido en el árbol compartido) |
| B07 | Nivel 1 · feed vertical de video | ✅ hecho | 2026-08-03 | `0107_1_b07_reproduccion.sql` **APLICADA** en `darma-dev`: `content_sessions` (RLS sin políticas, sin privilegios), `abrir_sesion_contenido()`, `latido_contenido()`, `completar_contenido()`, `barrer_sesiones_contenido()` (las cuatro `security definer`, solo `service_role`) y `feed_animo()` (`security invoker`, keyset). **Farmeo verificado CONTRA POSTGRES con rol `authenticated` real**: INSERT con `completed=true`, UPDATE de `completed`, UPDATE de `watched_seconds`, `SELECT content_sessions`, `award_karma()` y `completar_contenido()` → los seis `42501 permission denied`. Camino legítimo: 12 latidos de 5 s → 60 s acreditados → `acreditado:true, karma:1`, UN solo `karma_events`; repetido → `ya_completado`; con el tope diario agotado → `ok:true, karma:0, motivo:'tope_diario'`. Ráfaga de 12 latidos simultáneos → 0 s; un latido declarando 300 s → 7 s. `EXPLAIN` del feed: `Index Scan using idx_content_feed` + sonda por la PK de `content_views`, sin `Seq Scan`. `tsc`, `eslint` y 51 tests verdes. Datos de prueba borrados. Pendiente de medir: LCP y KB reales (exigen `next build`, prohibido en el árbol compartido) |
| B08 | Ingesta de contenido curado | 🟡 en curso | 2026-08-03 | Código completo y verde (`tsc`, `eslint`, 80 tests). Migración `0108_1_ingesta.sql` escrita pero **SIN APLICAR** (no hay base levantada), así que la ejecución real contra fuentes reales queda pendiente. Catálogo semilla: 5 fuentes (OMS, CDC, OPS) comprobadas en vivo con HTTP 200. YouTube se lee por feed Atom, sin clave ni cuota. `vercel.json` lo pide a F4 en `PEDIDOS.md` |
| B09 | Encuestas del feed | ✅ hecho | 2026-08-03 | `lib/polls/**` (8 archivos), las 5 rutas de `/api/polls/*` y `components/polls/**`. `tsc --noEmit --incremental false` limpio en TODO el repo, `eslint` limpio en lo propio, **67 pruebas verdes** (≈30 de camino de fallo). Migraciones `0109_1_b09_encuestas.sql` y `0109_2_b09_indice_descartes.sql` **APLICADAS** en `darma-dev`. **Verificado CONTRA POSTGRES con rol `authenticated` real (21/21):** voto duplicado con OTRA opción → `23505` y el `vote_count` de la opción no votada se queda en 0; opción de otra encuesta → `23503` (FK compuesta nueva); votar/descartar en nombre de otro, `update` y `delete` de `poll_votes`, `select` de `poll_options.vote_count`, `select` de `poll_bank`, `reponer_encuestas()`, poner mi `shown_today` a 0 e insertar una encuesta con `origin='banco'`/`min_reveal=3`/`total_votes=9999` → los nueve `42501`; votar en encuesta cerrada → `42501`; votos y cadencia ajenos → 0 filas. **Umbral:** con 4 votos `revelado:false` y `vote_count:null` en las tres opciones; con 7 (3/2/2) se revela y los contadores desnormalizados **cuadran exactamente** con las filas reales (3/2/2 y `total_votes=7`). Reposición: 0 activas → **3 activadas**; repetida → **0 activadas, sin duplicar**. **`EXPLAIN ANALYZE` con 1 000 007 `poll_votes`, 200 001 descartes y 50 015 encuestas: `Index Scan using idx_polls_activas_idioma` + `Index Only Scan` en las dos anti-joins, CERO `Seq Scan`, 14 buffers, 2,28 ms.** La consulta de resultados no toca `poll_votes` ni una vez. Datos de prueba borrados y `vacuum full`. ⚠️ **Merece revisión:** la migración cierra tres agujeros de `0002`/`0004` (INSERT abierto en `polls`/`poll_options`, voto que podía apuntar a la opción de otra encuesta, `vote_count` legible con la anon key) y reescribe tres políticas de `0002` para sacarles las subconsultas. ⚠️ Sin medir: KB de la tarjeta y LCP (exigen `next build`, prohibido en el árbol compartido). Pedidos abiertos: punto de inserción para B02, cron para F4, alias `Darma` para B01, formas impersonales en `lib/crisis.ts` para F3/B11, índice `user_id` en `ranking_snapshots` para B06 |
| B10 | Refugios y Almas Afines | ✅ hecho | 2026-08-03 | `lib/crypto/**`, `app/api/refuges/**`, `components/refuge/**`, `app/(app)/refugios/**` + migración **`0110_1_b10_claves.sql` APLICADA** en `darma-dev`. `tsc --noEmit --incremental false` y `eslint` limpios en lo propio, **51 pruebas verdes** (14 de fallo, incluida la nº 11 obligatoria: un campo `texto` en `/api/refuges/crisis` → 422 por `.strict()`). Cifrado **AES-256-GCM, nonce de 12 bytes aleatorios**, ECDH P-256 + HKDF-SHA256 con un sobre por miembro, privada `extractable:false` en IndexedDB. **Verificado CONTRA POSTGRES con tres sesiones reales (Ana, Luis, Intrusa): 30/30 en verde**, con dos controles positivos para descartar verdes falsos — una no miembro no ve el refugio, ni sus mensajes, ni quién está dentro, ni los sobres (`[]`, nunca 403); tras el bloqueo la sala desaparece para **las dos** partes, ana no puede escribir (42501), no puede guardarlo como alma afín ni abrir otra sala con él, y **no puede saber que la bloquearon**. ⚠️ **Fallo de seguridad encontrado y cerrado por `0110_1` §4:** las cinco tablas de refugio conservaban el INSERT íntegro para `authenticated` (mismo agujero que `0004`), lo que permitía falsificar `refuge_messages.created_at` —el trigger lo copia a `last_message_at` y una fecha del 2400 fija tu conversación arriba del todo para siempre en el móvil de la otra persona—, autoascenderse a `is_host`, e inventar contadores. ⚠️ **Fallo NO cerrado (no es mi archivo, en PEDIDOS):** `refuge_members_join` de `0002` deja que cualquiera se auto-inserte en un refugio si conoce su uuid. **Pérdida del dispositivo:** por defecto se pierde el historial, sin excepción; copia OPT-IN con frase de 12 palabras + PBKDF2 ≥ 600 000 (`check` en el esquema), y las tres advertencias literales en pantalla antes de enseñar la frase. `EXPLAIN ANALYZE` con **200 000 mensajes sembrados y borrados**: `Index Scan using idx_refuge_messages_keyset`, 4 buffers, 0,68 ms; página profunda del hilo 8 buffers, 3,8 ms; cero `Seq Scan` y cero `OFFSET`. `grep createAdminClient` en mis cuatro directorios: **cero**. Datos de prueba borrados (0 perfiles, 0 usuarios, 0 refugios). Pendiente de medir: KB de JS y LCP (exigen `next build`, prohibido en el árbol compartido) |
| B11 | Moderación e IA de crisis | 🟡 casi | 2026-08-03 | `lib/ai/**` (13 archivos), las 5 rutas de `/api/moderation/*` y el panel `/moderacion`. `tsc` y `eslint` limpios en lo propio, **52 pruebas verdes sin red y sin clave**. Verificado contra `darma-dev`: las dos colas usan su índice parcial (Index Scan, cero Seq Scan, 20 000 filas sembradas y borradas) y `crisis_events`/`moderation_flags` son ilegibles para `authenticated` (`permission denied`); `award_karma` ya tiene `execute` para `service_role` → **no hizo falta migración**. ⚠️ **NO hay `MODERATION_API_KEY` ni `@anthropic-ai/sdk` instalado**: hoy la app corre siempre en modo degradado (publica, no valida, escala el riesgo). Sin verificar hasta que exista la clave: el acierto real de la caché (`cache_read_input_tokens > 0`), la latencia real frente al timeout de 2 500 ms y la calidad de la rúbrica. Pedidos abiertos en PEDIDOS.md (SDK, 3 variables de entorno, cron de reproceso, rol de moderador en B19) |
| B12 | Economía premium | ⬜ pendiente | | Ola 4 |
| B13 | Push y PWA | ✅ hecho | 2026-08-03 | `lib/push/**` (11 archivos), las 5 rutas de `/api/push/*`, `components/pwa/**`, `public/sw.js` + `public/manifest.json` + dos iconos SVG. `tsc --noEmit --incremental false` limpio en lo propio (los 2 errores que quedan en el repo son de `app/api/privacy/**`, B20), `eslint` limpio, **69 pruebas verdes sin red, sin base y sin `web-push` instalado** (incluidas las de antiadicción, anonimato del contenido y 410 Gone). Migración `0131_b13_push.sql` **APLICADA** en `darma-dev` (rango de `PARALELO.md` §3; la ficha decía `0013`, ese número es de cimientos). **Aislamiento verificado CONTRA POSTGRES con dos sesiones `authenticated` reales**: `p256dh`/`auth`/`endpoint`/`user_agent_hash` son **ilegibles incluso para su dueño** (42501 — solo se conceden `id`, `created_at`, `last_ok_at`), un INSERT a nombre de otro da 42501, un `delete` SIN `where` solo alcanza 1 fila (la propia, la ajena sobrevive), las prefs ajenas devuelven 0 filas y un UPDATE masivo toca 1 fila, `push_dispatch_state` es ilegible (42501), y `is_blocked_between`/`destinatarios_alma_afin` dan 42501 desde una sesión (solo `service_role`). Bloqueo verificado: 1 destinatario sin bloqueo → 0 con bloqueo. `EXPLAIN`: `Bitmap Index Scan on idx_kindred_reverse` y `on idx_push_subscriptions_user`, sin `Seq Scan`. Datos de prueba borrados. ⚠️ **`web-push` NO está instalado y no hay llaves VAPID**: hoy la feature está apagada entera y en silencio (`/api/push/key` → `{publicKey:null}` con 200, opt-in no se pinta, envío no-op). ⚠️ **Una desviación consciente del contrato**: `alma_afin_en_crisis` ignora techo, agrupación y silencio, pero SÍ respeta un apagado explícito de ese tipo por parte de la persona. Pedidos abiertos: `manifest.json` en el matcher de `proxy.ts` (F4), mensaje al SW en el logout (B01), evento «te escucharon» (B04), `web-push` + `PUSH_UA_SALT`/VAPID en `package.json` y `.env.example` |
| B14 | Observabilidad y carga | 🟡 casi | 2026-08-03 | La sesión murió por un error de la API con el código ya escrito: `/api/health`, `/api/health/deep`, `/api/metrics`, `lib/observability/**`, `scripts/seed/**` y `scripts/load/**` (k6 + `EXPLAIN.md`). Compila, lint limpio, tests verdes. **Nada medido**: sin base de datos no hubo siembra de 1 M de filas, ni k6, ni planes reales. `EXPLAIN.md` tiene los huecos marcados como pendientes, sin números inventados. **Revisar antes de dar por cerrado** |
| B15 | Seguridad y CI | 🟡 casi | 2026-08-03 | La sesión murió por un error de la API. En disco: `scripts/security/**` (guards de cliente admin, economía y cabeceras) y `supabase/tests/*.sql` + `rls.integracion.ts`. **`.github/workflows/ci.yml` lo escribió la sesión de integración**, no B15. Falta `lib/supabase/database.types.ts` (se genera contra una base real). Los tests de RLS **no se han ejecutado nunca**: es lo más importante que queda pendiente del proyecto |
| B16 | Sistema de diseño | ✅ hecho | 2026-08-03 | La sesión se colgó tras escribir 9 de los 10 componentes; **`BotonCrisis.tsx` + su CSS los completó la sesión de integración** siguiendo el contrato de la ficha. `tsc` y `eslint` limpios, tests verdes. Enlace real a `/ayuda` (funciona sin JS), sin pulso ni rojo de alarma, `--danger` solo en borde e icono |
| B17 | Internacionalización | ✅ hecho | 2026-08-03 | `i18n/**` + `messages/**`. 58 pruebas en verde, `tsc` y `eslint` limpios en lo propio. Recursos de crisis **por país** (ES MX AR CO CL PE US GB + INTERNACIONAL); `recursosParaPais()` rechaza un locale por tipos. ⚠️ **Los 24 teléfonos están SIN verificar por un humano** (`tablaListaParaProduccion() === false`): ver PEDIDOS. next-intl aún no instalado; se usa `obtenerTraductor()` propio hasta que F4 monte el plugin |
| B18 | E2E Playwright | ⬜ pendiente | | Ola 4 |
| B19 | Centro de mando (admin) | ⬜ pendiente | | Ola 4 |
| B20 | Privacidad y RGPD | ✅ hecho | 2026-08-03 | `app/(legal)/**` (8 archivos, 7 páginas), `app/api/privacy/**` (4 rutas), `lib/privacy/**` (6 módulos + 5 de prueba). `tsc --noEmit --incremental false` limpio en TODO el repo, `eslint` limpio en lo propio, **60 pruebas verdes** (26 de camino de fallo). Migración **`0201_1_b20_privacidad.sql` APLICADA** en `darma-dev`. **BORRADO VERIFICADO CONTRA POSTGRES** con dos personas sembradas (A se borra, B es el tercero al que A acompañó), recuentos antes → después: `identity_vault` 1→**0**; alias `Faro Sereno 4821`→`alguien_19971dc1`; `bio` texto→null; `shadow_banned` false→true; gastable 197→0; cristales 12→0; `auth.users` 1→**0**; posts activos 1→0 y lápida 0→**1** con el texto de `TEXTO_LAPIDA_POST`; `retired_aliases` 0→1; `kindred` 1→0; `blocks.reason` texto→null; `moderation_flags.reporter_id` 1→**0**; refugios vivos 1→0. **Y lo de B sin moverse**: `reply_count` 1→**1**, comentario de A **con el mismo `body`**, `is_validated`/`is_helpful`/`state` intactos, karma de B 300→300, su post `active`. **Conservado seudonimizado**: `karma_events` 2→2, `crystal_ledger` 1→1, `crisis_events` 1→1. **Idempotencia**: segunda pasada sin error y con el mismo resultado (`ya_estaba_borrado:true`). **Atomicidad**: forzado un fallo en el paso 7 con una FK RESTRICT temporal → la transacción entera revirtió, `identity_vault` seguía con su fila y el perfil intacto (nadie a medio borrar). **Dos pasos**: token equivocado, persona ajena y token reutilizado → `false`; el correcto → `true` y suspensión inmediata. Cancelación verificada, incluida la rama que **no** levanta el shadow-ban si hay señal de moderación abierta. Datos de prueba borrados (`where` comprobado antes de cada delete; la base estaba compartida con B13 sembrando 100 000 perfiles a la vez). ⚠️ **Merece revisión: la migración ELIMINA la FK `profiles.id → auth.users on delete cascade`** —destruía comentarios de terceros, `crystal_ledger` y `crisis_events` al ejercer el derecho de supresión— y la sustituye por un trigger que cubre el lado INSERT. ⚠️ **Sin cron, ningún borrado confirmado se ejecuta**: `borrados_vencidos()` y `purgar_retencion()` existen y nadie las llama (pedido a B08). Deuda de traducción de `app/(legal)/**` anotada en PEDIDOS. ⚠️ La ÚLTIMA línea de la migración (`revoke all on function profiles_exige_auth_user()`, detectada por el linter de Supabase) está en el archivo pero **no aplicada**: la base pasó a SOLO LECTURA (750 MB) mientras otro bloque sembraba en masa |

Leyenda: ⬜ pendiente · 🟡 en curso · ✅ hecho · 🔴 bloqueado (di por qué en Notas)
